import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact for Service Support",
  description: "Contact An Khai Travel for hotels, visa support, car rental, airport transfer, spa, SIM, flight tickets, guides, and restaurants.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
